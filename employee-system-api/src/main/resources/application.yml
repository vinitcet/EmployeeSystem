server:
  port: 9001
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/employee_system
    username: epace
    password: epace
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
